{% extends 'base.html.twig' %}

{% block title %}FormuleDetail{% endblock %}

{% block body %}
    <h1>FormuleDetail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formule_detail.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ formule_detail.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('formule_detail_index') }}">back to list</a>

    <a href="{{ path('formule_detail_edit', {'id': formule_detail.id}) }}">edit</a>

    {{ include('formule_detail/_delete_form.html.twig') }}
{% endblock %}
