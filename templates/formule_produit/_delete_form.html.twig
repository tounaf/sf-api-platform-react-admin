<form method="post" action="{{ path('formule_produit_delete', {'id': formule_produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formule_produit.id) }}">
    <button class="btn">Delete</button>
</form>
