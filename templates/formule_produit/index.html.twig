{% extends 'base.html.twig' %}

{% block title %}FormuleProduit index{% endblock %}

{% block body %}
    <h1>FormuleProduit index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for formule_produit in formule_produits %}
            <tr>
                <td>{{ formule_produit.id }}</td>
                <td>
                    <a href="{{ path('formule_produit_show', {'id': formule_produit.id}) }}">show</a>
                    <a href="{{ path('formule_produit_edit', {'id': formule_produit.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('formule_produit_new') }}">Create new</a>
{% endblock %}
