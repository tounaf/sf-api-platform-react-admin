{% extends 'base.html.twig' %}

{% block title %}FormuleProduit{% endblock %}

{% block body %}
    <h1>FormuleProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formule_produit.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('formule_produit_index') }}">back to list</a>

    <a href="{{ path('formule_produit_edit', {'id': formule_produit.id}) }}">edit</a>

    {{ include('formule_produit/_delete_form.html.twig') }}
{% endblock %}
