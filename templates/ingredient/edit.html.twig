{% extends 'base.html.twig' %}

{% block title %}Edit Ingredient{% endblock %}

{% block body %}


    <div class="container-fluid pl-5">
        <div class="card">
            <h1 class="card-header">Editer Ingredient</h1>
            <div class="card-body">
				{{ include('ingredient/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
            <div class="card-footer">
                <a class="btn btn-success" href="{{ path('ingredient_index') }}">Nouveau</a>
                <a class="btn btn-info" href="{{ path('ingredient_index') }}">Retour à la liste</a>
            </div>
        </div>
    </div>


{% endblock %}
